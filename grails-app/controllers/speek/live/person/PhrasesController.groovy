package speek.live.person

import com.speek.grammar.Phrase
import com.speek.grammar.Meaning

class PhrasesController
{

    //def index = { }
    def scaffold = true;

   /* def createPhrase = {
            Phrase phrase = phrase.findByText(params.phraseText);
            if ( phrase == null)  {
                 phrase = new Phrase(params.phraseText);

                 Meaning meaning = Meaning.findByText(params.meaningText);
                 if (meaning == null) {
                    meaning = new Meaning(text:params.meaningText);
                    meaning.save();
                 }
                 phrase.setMeaning(meaning);
            }
    }

    def deletePhrase =  {
         Phrase phrase = phrase.findById(params.phraseId);
         if (phrase != null)
             phrase.delete();
    }

    def updatePhrase = {
        Phrase phrase = Phrase.get(params.phraseId);
        if (phrase != null)
        {
            Meaning meaning = Meaning.get(params.meaningId);
            if (meaning
               phrase.set
            }

        }

    }

    def getPhrases = {

    }

    def grxml = {


    }  */
}
